# QA 체크리스트

배포 없이 로컬(`docker compose` + `pnpm dev`)에서 전 기능을 확인하기 위한 수동 테스트 절차입니다. 셋업은 [README.md](./README.md)를 먼저 따라 하세요 (`docker compose up -d` → `migrate` → `seed_real_hackathons`).

## 사전 준비

### 카카오 로그인 없이 로그인하기 (개발 전용)

실제 카카오 로그인도 되지만, 여러 계정을 오가며 테스트할 땐 아래 방법이 훨씬 빠릅니다.

```bash
docker compose exec backend uv run python manage.py issue_token <email>
```

출력된 `access`/`refresh` 값을 아래 URL에 붙여 접속하면 로그인됩니다 (`AuthCallbackScreen`이 카카오 콜백과 동일한 경로로 처리):

```
http://localhost:8443/#/auth/callback?access=<ACCESS_TOKEN>&refresh=<REFRESH_TOKEN>
```

`issue_token`은 이미 존재하는 계정만 됩니다. 테스트용 계정이 필요하면:

```bash
docker compose exec backend uv run python manage.py seed_recommendation_demo
```

"AI 추천 테스트용 해커톤"에 참가자로 등록된 더미 유저 `demo_rec_1@test.local` ~ `demo_rec_17@test.local`가 생성됩니다 (재실행해도 중복 생성 안 됨). 프로필까지 채워져 있어 추천/커피챗 테스트에 바로 쓸 수 있습니다.

### 여러 계정 동시 테스트 (2세션)

커피챗·리뷰·수동 참가자(회원 매칭)처럼 상대방 계정이 필요한 기능은 브라우저 세션을 두 개로 분리해야 합니다. 같은 origin(`localhost:8443`)은 창을 새로 열어도 localStorage를 공유하므로, **일반 창 + 시크릿(Incognito) 창** 조합을 씁니다. 각 창에서 위 로그인 방법으로 서로 다른 계정에 로그인하면 됩니다.

> `issue_token`은 개발 전용 명령이라 프로덕션에는 없습니다 — Kakao OAuth 없이 화면/API를 검증할 때만 사용하세요.

---

## 1. 로그인 / 인증

- [ ] `/login`에서 "카카오로 시작하기" 클릭 → 카카오 인가 화면으로 리다이렉트됨
- [ ] "로그인 없이 해커톤 둘러보기" 클릭 → 비로그인 상태로 `/hackathons` 진입 (검색/카테고리 필터는 되지만 참가·추천 등은 로그인 유도)
- [ ] 이미 로그인된 상태로 `/login` 접근 시 자동으로 `/hackathons`로 리다이렉트
- [ ] 로그인 필요 화면(`/mypage`, `/coffeechats` 등)에 비로그인으로 접근 → `/login`으로 튕김
- [ ] `#/auth/callback?access=...&refresh=...`로 진입 시 정상 로그인 + `/hackathons`로 이동
- [ ] `#/auth/callback?error=...`(카카오 로그인 취소 시나리오) → "로그인에 실패했어요" 화면 + "다시 로그인하기" 버튼
- [ ] `마이페이지` → "로그아웃" 클릭 → 토큰 제거되고 로그인 필요 화면 접근 시 다시 로그인 유도

## 2. 홈 / 해커톤 탐색 (`/hackathons`)

- [ ] 실제 등록된 6개 해커톤(더미 4개는 제외)이 카드로 노출됨, 배너 이미지가 정상 표시됨
- [ ] 검색창에 제목/카테고리 일부 입력 → 디바운스 후 결과 필터링 (예: "해커톤" 검색)
- [ ] 카테고리 칩(`전체`/`AI`/`모바일`/`클라우드`/`DevOps`) 클릭 → 해당 카테고리만 노출, "전체"는 필터 해제
- [ ] 검색어+카테고리 동시 적용 시 둘 다 만족하는 결과만 노출
- [ ] 검색 결과 없음 → "검색 결과가 없어요" 빈 상태 노출
- [ ] 카드 클릭 → 해커톤 상세(`/hackathons/:id`)로 이동

## 3. 해커톤 상세 (`/hackathons/:id`)

- [ ] 비로그인 상태에서도 상세 정보(배너/카테고리/제목/기간/설명/참가자·팀 수) 조회 가능
- [ ] "이 해커톤 참가하기" 클릭 → 비로그인이면 로그인 화면으로, 로그인 상태면 `/hackathons/:id/join`으로 이동
- [ ] 존재하지 않는 id로 접근 시 `ErrorState` + 재시도 버튼 노출

## 4. 참가 신청 (`/hackathons/:id/join`)

- [ ] "개인으로 참가" 클릭 → 참가 처리 후 `/hackathons/:id/profile-setup`으로 이동
- [ ] "팀 모집자로 참가" 클릭 → 참가 처리 후 `/hackathons/:id/team-setup`으로 이동
- [ ] 이미 참가한 해커톤에 다시 진입 시 중복 참가 에러가 `InlineError`로 노출됨
- [ ] 요청 처리 중 버튼이 비활성화되어 중복 클릭 방지되는지

## 5. 개인 프로필 작성 (`/hackathons/:id/profile-setup`, `/profile`)

- [ ] 대표 역할/기술스택/선호지역/관심분야는 다중 선택, 활동가능시간/참여목표/협업방식/소통방식은 단일 선택으로 동작
- [ ] 한 줄 소개 미입력 상태 → 저장 버튼 비활성화 + 안내 문구 노출
- [ ] 역할을 하나도 선택 안 한 상태 → 저장 버튼 비활성화 (역할 우선 안내)
- [ ] 상세 자기소개 5문항 아코디언 중 하나라도 비어있으면 저장 버튼 비활성화
- [ ] 5문항 + 역할 + 한줄소개를 모두 채우면 저장 버튼 활성화
- [ ] 포트폴리오 링크 추가(타입+URL) / 삭제 동작
- [ ] "추천 대상에서 비공개" 토글 on/off 저장 확인
- [ ] `/hackathons/:id/profile-setup`(해커톤 컨텍스트 있음)에서 저장 시 버튼 라벨 "저장하고 추천받기" + 저장 후 AI 추천이 트리거되는지(추천 화면에서 결과 확인)
- [ ] `/profile`(마이페이지 진입, 해커톤 컨텍스트 없음)에서는 버튼 라벨 "프로필 저장"이고 추천 트리거가 없는지
- [ ] 신규 유저(프로필 없음)로 진입 시 404가 에러로 뜨지 않고 빈 폼으로 시작하는지

## 6. 팀 모집 조건 (`/hackathons/:id/team-setup`, `/teams/:id/edit`)

- [ ] 필요 역할별 인원수를 하나도 안 채운 상태 → "저장하고 추천받기" 버튼 비활성화
- [ ] 최소 1개 역할에 인원수 설정 시 버튼 활성화, 저장 후 팀 생성 + AI 추천 트리거
- [ ] `팀 편집` 화면에서 모집 상태 드롭다운(모집 중/매칭 완료/재모집/모집 마감/비공개) 값 변경 후 저장 → 목록/상세에 상태 배지가 바뀌어 반영되는지
- [ ] "수정 저장" 클릭 시 토스트 노출 후 `/my/status`로 이동
- [ ] "취소" 클릭 시 변경사항 버리고 `/my/status`로 이동
- [ ] 본인이 만든 팀이 아닌 `teams/:id/edit`에 접근 시 권한 에러 처리되는지

## 7. AI 추천 (`/hackathons/:id/recommendations`)

- [ ] 추천 생성 중 스켈레톤 + "AI가 최적의 팀원을 찾고 있어요…" 노출, 완료 후 자동 전환
- [ ] 상위 5명은 AI 문구 카드(잘 맞는 점/상호 보완/체크 포인트/추천 이유), 그 외는 가로 스크롤 카드로 구분되어 보이는지
- [ ] 상위 카드의 "넘기기" 클릭 → 해당 사람이 맨 뒤로 밀리고 6등이 5등 자리로 승격 (AI 문구 없는 사람이 올라오면 자동으로 컴팩트 카드로 렌더링되는지)
- [ ] "넘기기"는 새로고침/"다시 추천받기" 시 순서가 초기화되는지 (로컬 상태만 유지하는 설계 확인)
- [ ] "커피챗 신청하기" → 모달에서 신청 완료 시 버튼이 "신청함 ✓"으로 바뀌는지
- [ ] "다시 추천받기" 클릭 시 재생성되고 로딩 상태 노출
- [ ] 프로필 미작성/비공개 유저는 후보에서 제외되는지 (`seed_recommendation_demo`의 무프로필유저/강나은 케이스로 확인)
- [ ] 추천 결과가 하나도 없을 때 "아직 조건에 맞는 추천이 없어요" 빈 상태 + "프로필 수정하기" CTA

## 8. 상대 프로필 상세 (`/users/:id`)

- [ ] 자기소개 5문항, 역할/기술/관심사, 활동시간·목표·협업·소통 정보 노출
- [ ] 포트폴리오 링크 클릭 시 새 탭에서 열리는지
- [ ] 커피챗 수락 전에는 오픈채팅/연락처가 마스킹("커피챗 수락 후 공개")되는지, 수락 후에는 공개되는지
- [ ] 해커톤 컨텍스트 없이(다른 화면에서 바로) 진입 시 "커피챗 신청하기" 버튼이 비활성화 + "해커톤을 먼저 선택해주세요" 툴팁
- [ ] 이미 신청 보낸 상대에게는 "커피챗 신청함" 배지로 바뀌는지

## 9. 커피챗

### 9-1. 요청 / 수락 / 거절 (단일 세션으로도 상태 전이는 확인 가능)

- [ ] `/coffeechats` "받은 신청" 탭에서 pending 건 "수락" 클릭 → `/coffeechats/:id/matched`로 이동, 채팅 스레드 생성
- [ ] pending 건 "거절" 클릭 → 목록에서 상태가 즉시 반영되는지 (거절 필터에서 확인)
- [ ] 수락/진행중 건에서 "진행중으로 표시" / "완료로 표시" 버튼으로 상태 전이가 되는지
- [ ] 🗑 삭제 클릭 → 확인창(confirm) → 삭제 후 목록에서 사라지는지
- [ ] 받은 신청 목록에는 accepted/in_progress/completed 건이 안 보이고(팀 스페이스로 이동됨), pending/rejected만 보이는지
- [ ] "보낸 신청" 탭에서 상태별 필터(전체/대기/수락/거절 등)가 정상 동작하는지
- [ ] 상대가 오픈채팅 링크를 남긴 경우 "💬 오픈채팅 링크 열기"가 노출되는지

### 9-2. 상호 대화 테스트 (배포 없이 로컬 2세션)

커피챗은 두 사용자가 각각 요청/수락을 해야 채팅방이 열리는 구조라, 제대로 동작하는지 확인하려면 서로 다른 계정으로 로그인한 브라우저 세션이 두 개 필요합니다.

**1. 더미 유저 준비** — [사전 준비](#사전-준비)의 `seed_recommendation_demo` 실행, 서로 다른 두 계정(`demo_rec_1@test.local`, `demo_rec_2@test.local` 등) 선택.

**2. 두 세션 로그인** — [사전 준비](#사전-준비)의 방법으로 세션 A·B 각각 다른 계정으로 로그인.

**3. 요청 → 수락 → 채팅 확인**

- [ ] 세션 A: `#/hackathons/<id>/recommendations` 또는 상대 프로필(`#/users/<id>`)에서 "커피챗 신청하기" 클릭
- [ ] 세션 B: `#/coffeechats` (받은 요청함)에서 요청을 수락 → 이 시점에 `ChatThread`가 생성됨
- [ ] 두 세션 모두 `#/messages`로 이동해 새로 생긴 스레드를 열고 서로 메시지를 주고받으며 상대 쪽에 정상 도착하는지, 읽음 처리(`read_at`)가 반영되는지 확인

## 10. 채팅

- [ ] `/messages` 목록에서 읽지 않은 메시지가 굵게/배지로 표시되는지
- [ ] 대화방 진입 시 자동으로 읽음 처리되고, 상대 목록의 안읽음 배지가 줄어드는지
- [ ] 메시지 입력 후 Enter로 전송, Shift+Enter로 줄바꿈되는지
- [ ] 전송 중 스피너, 빈 입력일 때 전송 버튼 비활성화
- [ ] 새 메시지 수신 시 폴링으로 자동 반영되고 하단으로 자동 스크롤되는지
- [ ] 날짜가 바뀌면 구분선이 표시되는지
- [ ] 헤더 "프로필 보기" → 상대 프로필로 이동
- [ ] 대화가 없는 스레드 목록/빈 대화방에서 각각의 빈 상태 문구 노출

## 11. 내 현황 / 팀 스페이스 (`/my/status`, `/my/status/:id`)

- [ ] `/my/status`에서 참가 중인 해커톤 목록, 개인/팀 모집자 라벨, 상태 배지가 맞게 보이는지
- [ ] 종료된 참가는 "✅ 종료됨"으로 표시되는지
- [ ] 팀 트랙 생성자에게만 "모집 조건 수정" 버튼이 보이는지
- [ ] 팀 스페이스에서 커피챗으로 맺어진 팀원과 수동 추가한 참가자가 한 목록에 시간순으로 섞여 보이는지
- [ ] 전화번호로 수동 참가자 추가 시, 등록된 회원과 일치하면 바로 추가되는지 (`is_member` 배지 확인)
- [ ] 전화번호가 회원과 매칭되지 않으면(`not_member`) 이름/이메일 입력 폼이 추가로 나타나고, 둘 다 입력해야 "추가" 버튼이 활성화되는지
- [ ] 팀원 카드에서 "대화 열기"(스레드 있을 때만), "진행중으로 표시" 동작
- [ ] "⭐ 리뷰 작성하기" → 별점(1~5) + 500자 이내 텍스트 작성 후 저장, 이미 작성한 경우 "✏️ 리뷰 수정하기"로 바뀌는지
- [ ] 팀원/수동 참가자 🗑 삭제 시 확인창 노출 후 삭제
- [ ] "내 할 일"에 입력 후 Enter 또는 "추가"로 등록, 체크박스로 완료 토글, 🗑로 삭제
- [ ] 할 일이 없을 때 빈 상태 문구 노출
- [ ] "프로젝트 종료하기" → 확인창 → 종료 후 "✅ 종료된 프로젝트예요" 노출로 바뀌고 관련 조작이 막히는지

## 12. 마이페이지 / 리뷰

- [ ] `/mypage`에서 프로필 요약(아바타/이름/이메일), 비공개 상태면 "현재 추천 목록에 노출되지 않음" 배지 확인
- [ ] "프로필 수정" → `/profile`로 이동해 수정 가능
- [ ] "추천 대상에서 비공개" 토글 시 즉시 저장 + 토스트 노출, 실제로 추천 화면에서 제외되는지 교차 확인
- [ ] "⭐ 리뷰 보기" → `/mypage/reviews`에서 평균 평점/개수, 개별 리뷰 별점이 보이는지
- [ ] 받은 리뷰가 없을 때 빈 상태 문구
- [ ] "로그아웃" 클릭 시 세션 종료 확인

## 13. 알림 (`/notifications`)

- [ ] 안읽은 알림 클릭 시 즉시 읽음 처리(배지 수 감소)되고, 알림 타입에 맞는 화면으로 이동하는지 (커피챗 요청함/성사/AI 추천/메시지/상대 프로필 등)
- [ ] `target_id`가 없는 경우에도 대체 경로로 자연스럽게 이동하는지(예: AI 추천 알림은 최근 본 해커톤 기준으로 이동)
- [ ] "모두 읽음 처리" 클릭 시 전체 배지가 0이 되는지, 실패 시 원상복구(rollback)되는지
- [ ] 알림이 없을 때 빈 상태 문구
- [ ] 상단 네비게이션 바의 알림 배지 수가 실제 안읽은 알림 수와 일치하는지

## 14. 공통 / 회귀

- [ ] 상단 네비게이션(홈/추천/커피챗/내 현황/마이페이지) 탭이 현재 경로에 맞게 활성화 표시되는지
- [ ] "추천" 탭 클릭 시 마지막으로 본 해커톤이 없으면 `/hackathons`로, 있으면 해당 추천 화면으로 이동하는지
- [ ] 존재하지 않는 경로 접근 시 404 화면("페이지를 찾을 수 없어요" + "홈으로 가기") 노출
- [ ] 로고 이미지가 `/`(랜딩)와 `/hackathons`(홈) 양쪽에서 동일한 크기로 보이는지
- [ ] 백엔드가 꺼진 상태에서 각 화면 진입 시 "서버에 연결할 수 없어요" 류의 `ErrorState` + 재시도 버튼이 뜨는지 (`docker compose stop backend`로 재현 가능)
